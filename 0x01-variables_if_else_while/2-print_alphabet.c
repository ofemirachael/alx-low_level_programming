#include <stdio.h>
/**
 * main - print the alphabet in lowercase, followed by a new line
 *
 * Return: Always 0
 */
int main(void)
{
	char alphabet;

	for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
	{
		putchar(aiphabet);
	}
	putchar('\n');
	return (0);
}
